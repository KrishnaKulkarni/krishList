<%= header(@header_options) %>
<main class="account-responses">
  <%= account_header(@user) %>

  <section class="responses-container b-on-y">
    <h4 class="item-list-heading">Ad: <%= @ad.title %> - All Comments
		</h4>
    <ul class="account-item-list b-on-y">
      <%= render @responses %>
    </ul>
  </section>

</main>